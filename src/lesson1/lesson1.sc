import scala.math._

val a = 6 * 7
val b : BigInt = 6 * 7
b.pow(100)

b pow 100

sqrt(10)

1.to(10)

1 to 10

1 to 10 map(sqrt(_))

1.to(10).map(sqrt(_))

val string1 = "Mississipi"

string1.distinct

var string2 = "Rhine"
string2.permutations.toArray


"ABC".sum.toInt